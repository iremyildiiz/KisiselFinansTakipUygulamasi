import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: { flex: 1, padding: 10, backgroundColor: '#e5e2c5ff' },
  input: {
    marginBottom: 10,
  },
  button: {
    marginTop: 10,
    borderRadius: 6,
  },
});
